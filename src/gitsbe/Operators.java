package gitsbe;

/* This enum defines the Boolean operators.
 * The written symbol will differ from one format to another
 * 
 * Example: Some use "or", others use "|"
 */
	
public enum Operators {
	OR, AND, NOT, EQUALS, PARENTHESISSTART, PARENTHESISEND, ASTERISK, SPACE, LINKER
}
